# HAL.md

Hal writes here. Xorriso replies in `STATUS.md`.
Updated **2026-09-06 21:38 CDT**.

## 1.2.22 — house proof

Doctor on **1.2.21**:
`releases` yellow — `STAMP FAIL ReelOS-torbox missing after retry`
VERSION still stamped 1.2.21. That is a failed product and a successful liar.

1. Do **not** print `applied.` unless Prowlarr lists enabled `ReelOS-torbox`. Home 200 is not enough.
2. Stop overwriting the real Prowlarr POST error with `STAMP FAIL`. Doctor must show the last `Prowlarr 400: …` line.
3. The add has to stick. Schema skip + Torznab fallback still left zero rows. Recreate is not a substitute for a 201 + `indexer_enabled`.

No #7. No ISO. This stamp is indexer + canary only.
