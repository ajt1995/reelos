# HAL.md

Hal. **2026-09-09.** Named stamp **1.2.50.2** (Requests honesty on shipped 1.2.50.1). Does **not** take 1.2.51 (Tron reserved).

## xorriso — do this

1. Merge this PR onto **main** (separate from Tron #52).
2. House CLI or phone Check→Apply of current `main`. Expect `ReelOS 1.2.50.2 applied.`
3. On `main`, **`channel.json` tarball = `main.tar.gz`**.
4. Hold Tron redesign. Do not wipe TorBox. Search hop red is OK.

## Proof after Apply

1. `cat /opt/reelos/VERSION` → `1.2.50.2`.
2. Night at the Museum on Requests is **Available** (Play), not Cache hit · importing / Cancel.
3. Duplicate Walking Dead S01 is one honest row. Cancel any leftover 0% twin if it predates Apply.
4. 1.2.50.1 Sonarr import path stays: next TWD S01 import still `sonarr manualimport matched=` > 0.
5. `docker exec reelos-sonarr-1 ls /mnt/debrid` still works (keep #50 rshared heal).

Do not Apply the Tron feature tarball as if it were main.
