# Development entry point

This file previously described an obsolete deployment process. It is retained
only so older links do not send a contributor into stale instructions.

Read [AGENTS.md](AGENTS.md), then run:

```powershell
npm run context:brief
```

Current scope, architecture, workstream ownership, validation commands, and
release truth are linked from that single entry point. Do not infer completion
from a version number, a reachable Home route, or historical documentation.
