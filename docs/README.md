# docs

ReelOS is the OS. Ubuntu is what it sits on.

- Doctor: Settings → Doctor, or `python3 /opt/reelos/bin/reelos-doctor.py`
- Updates: GitHub `ajt1995/reelos`. Not this chat.
- Indexers: you add them. The disc ships empty.
